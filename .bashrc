[ -n "$PS1" ] && source ~/.bash_profile

# added by travis gem
[ -f /Users/zmartineau/.travis/travis.sh ] && source /Users/zmartineau/.travis/travis.sh

### Added by the Heroku Toolbelt
export PATH="/usr/local/heroku/bin:$PATH"
